# Cópia este arquivo para .env.test ou rode: yarn test:e2e:setup
# Os testes usam .env.local se .env.test não existir.
#
# Os testes mobile desbloqueiam o app via cadastro+PIN automaticamente.
# Exige NEXT_PUBLIC_SUPABASE_* e SUPABASE_SERVICE_ROLE_KEY.
#
# Para testes de chat (troca de mensagens, scroll, etc.), defina E2E_TEST_RECIPIENT_NICKNAME
# com o nickname de um usuário existente no Supabase. Os testes tentarão adicioná-lo
# quando a lista estiver vazia. Crie esse usuário manualmente uma vez.

# Supabase (obrigatório para testes de auth, chat e mensagens)
NEXT_PUBLIC_SUPABASE_URL=https://seu-projeto.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sua-anon-key
SUPABASE_SERVICE_ROLE_KEY=sua-service-role-key

# News API (opcional - portal de notícias)
NEXT_PUBLIC_NEWS_API_KEY=opcional

# Push (opcional)
NEXT_PUBLIC_VAPID_PUBLIC_KEY=opcional
VAPID_PRIVATE_KEY=opcional

# Contato de teste (opcional - para rodar testes de chat)
# Nickname de um usuário existente; os testes o adicionam quando não há chats
# E2E_TEST_RECIPIENT_NICKNAME=e2e_recipient

# URL base dos testes (opcional, default: http://localhost:3005)
# PLAYWRIGHT_BASE_URL=http://localhost:3005
